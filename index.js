'use strict';

// imports the whole exported object
// const start = require('./server.js');
// start.start();

// imports the single method inside the exported object
const { start } = require('./server.js');

start();
